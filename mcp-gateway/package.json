{
  "name": "teamos-mcp-gateway",
  "version": "1.0.0",
  "description": "HTTP gateway sidecar bridging Django agent to MCP stdio tool servers",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
